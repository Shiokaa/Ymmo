---
# Dépendances de collections Ansible pour le projet Ymmo-Infra
# Installer avec : ansible-galaxy collection install -r collections/requirements.yml
collections:
  - name: oxlorg.opnsense
    version: "25.7.8"
  # Modules Windows génériques (win_dns_client, win_hostname, win_reboot…)
  - name: ansible.windows
    version: "3.5.0"
  # Jonction au domaine Active Directory (microsoft.ad.membership)
  - name: microsoft.ad
    version: "1.11.0"
