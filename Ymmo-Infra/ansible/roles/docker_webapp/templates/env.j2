# Fichier .env généré par Ansible (rôle docker_webapp) — NE PAS éditer manuellement.
# Variables attendues par ../Ymmo-Web/docker-compose.yml.

DB_PORT={{ webapp_db_port }}
DB_USER={{ webapp_db_user }}
DB_PASSWORD={{ webapp_db_password }}
DB_NAME={{ webapp_db_name }}
JWT_SECRET={{ webapp_jwt_secret }}
